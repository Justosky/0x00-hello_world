#include "main.h"

/*
 *_islower - A function that checks if qa character is lowercase*
 *@c: the character to be checked if it is lower alphabet.
 *Return: Returns an interger.*/

int _islower(int c)
{
	return (0);
}
