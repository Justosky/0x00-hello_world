#include "main.h"

/*
 *_memcpy - A function that copies src to dest*
 *@dest: desination 
 *@src: source
 *@n: the number of character from src that will be copied to dest.*/

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (0);
}
