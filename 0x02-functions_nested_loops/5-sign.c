#include "main.h"

/**
*print_sign - Print the sign of a number
Return: Always 0 (SUCCESS)
*/

int print_sign(int n)
{
	if (n > 0)
	{
	_putchar('+');
	_putchar('n')}

	else if (n < 0)
	{
	_putchar('-'); }

	else {
	_putchar('0'); }

return(0);


}
