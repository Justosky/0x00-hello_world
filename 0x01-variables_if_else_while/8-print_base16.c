#include <stdio.h>

/**
*main - Entry
*Return: Always 0 (SUCCESS)
*/

int main(void)
{
	int num_of_base_sikstin;

	for (num_of_base_sikstin = 48; num_of_base_sikstin < 58;
	num_of_base_sikstin++)
	{putchar(num_of_base_sikstin); }

	for (num_of_base_sikstin = 97; num_of_base_sikstin <= 102;
	 num_of_base_sikstin++)
	{putchar(num_of_base_sikstin); }

	putchar('\n');

	return (0);
}
